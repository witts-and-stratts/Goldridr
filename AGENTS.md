# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Prefer `@expo/ui` components over `react-native` primitives whenever Expo UI provides a matching universal component. Keep `react-native` for layout and host primitives that Expo UI does not replace cleanly. `src/app/manage/discounts.tsx` is the reference pattern for this project.
