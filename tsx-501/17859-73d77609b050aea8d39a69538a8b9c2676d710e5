{"code":"__filename=\"/Volumes/Dev/www/Goldridr/website/src/lib/notifications/sms-consent.ts\";(()=>{\n\"use strict\";var __defProp=Object.defineProperty;var __getOwnPropDesc=Object.getOwnPropertyDescriptor;var __getOwnPropNames=Object.getOwnPropertyNames;var __hasOwnProp=Object.prototype.hasOwnProperty;var __name=(target,value)=>__defProp(target,\"name\",{value,configurable:true});var __export=(target,all)=>{for(var name in all)__defProp(target,name,{get:all[name],enumerable:true})};var __copyProps=(to,from,except,desc)=>{if(from&&typeof from===\"object\"||typeof from===\"function\"){for(let key of __getOwnPropNames(from))if(!__hasOwnProp.call(to,key)&&key!==except)__defProp(to,key,{get:()=>from[key],enumerable:!(desc=__getOwnPropDesc(from,key))||desc.enumerable})}return to};var __toCommonJS=mod=>__copyProps(__defProp({},\"__esModule\",{value:true}),mod);var sms_consent_exports={};__export(sms_consent_exports,{classifyInboundKeyword:()=>classifyInboundKeyword,hasActiveSmsConsent:()=>hasActiveSmsConsent,normalizePhone:()=>normalizePhone,recordSmsConsent:()=>recordSmsConsent,restoreSmsConsent:()=>restoreSmsConsent,revokeSmsConsent:()=>revokeSmsConsent});module.exports=__toCommonJS(sms_consent_exports);var import_client=require(\"@/lib/pocketbase/client\");var import_collections=require(\"@/lib/pocketbase/collections\");var import_core=require(\"@/lib/pocketbase/core\");const OPT_OUT_KEYWORDS=new Set([\"stop\",\"stopall\",\"unsubscribe\",\"cancel\",\"end\",\"quit\",\"revoke\",\"optout\"]);const OPT_IN_KEYWORDS=new Set([\"start\",\"unstop\",\"yes\",\"optin\"]);const HELP_KEYWORDS=new Set([\"help\",\"info\"]);function classifyInboundKeyword(body){const keyword=body.trim().toLowerCase().replace(/[^a-z]/g,\"\");if(OPT_OUT_KEYWORDS.has(keyword))return\"opt_out\";if(OPT_IN_KEYWORDS.has(keyword))return\"opt_in\";if(HELP_KEYWORDS.has(keyword))return\"help\";return\"unknown\"}__name(classifyInboundKeyword,\"classifyInboundKeyword\");function normalizePhone(phone){const digits=phone.replace(/\\D/g,\"\");return digits.length>10?digits.slice(-10):digits}__name(normalizePhone,\"normalizePhone\");async function bookingsForPhone(phone){const normalized=normalizePhone(phone);if(normalized.length<10)return[];const pb=(0,import_client.getPocketBaseClient)();const records=await pb.collection(import_collections.pocketBaseCollections.bookings).getFullList({filter:pb.filter(\"passengerPhone != ''\"),fields:\"id,passengerPhone\"});return records.map(record=>({id:String(record.id),passengerPhone:String(record.passengerPhone||\"\")})).filter(record=>normalizePhone(record.passengerPhone)===normalized)}__name(bookingsForPhone,\"bookingsForPhone\");async function recordSmsConsent(input){const normalized=normalizePhone(input.phone);if(!normalized||!input.customerEmail)return;const consentedAt=input.consentedAt||new Date().toISOString();const existing=await(0,import_core.first)(import_collections.pocketBaseCollections.smsConsents,\"phone = {:phone} && customerEmail = {:email}\",{phone:normalized,email:input.customerEmail});if(existing){await(0,import_client.getPocketBaseClient)().collection(import_collections.pocketBaseCollections.smsConsents).update(existing.id,{consentVersion:input.consentVersion,consentedAt,revokedAt:\"\"});return}await(0,import_core.createWithLegacyId)(import_collections.pocketBaseCollections.smsConsents,{customerEmail:input.customerEmail,phone:normalized,consentVersion:input.consentVersion,consentedAt,revokedAt:\"\",sourceCreatedAt:consentedAt})}__name(recordSmsConsent,\"recordSmsConsent\");async function revokeSmsConsent(phone){const normalized=normalizePhone(phone);if(!normalized)return{ledgerRevoked:0,bookingsCleared:0};const pb=(0,import_client.getPocketBaseClient)();const revokedAt=new Date().toISOString();const ledger=await pb.collection(import_collections.pocketBaseCollections.smsConsents).getFullList({filter:pb.filter(\"phone = {:phone}\",{phone:normalized})});for(const record of ledger){await pb.collection(import_collections.pocketBaseCollections.smsConsents).update(record.id,{revokedAt})}const bookings=await bookingsForPhone(normalized);for(const booking of bookings){await pb.collection(import_collections.pocketBaseCollections.bookings).update(booking.id,{smsConsentedAt:\"\"})}return{ledgerRevoked:ledger.length,bookingsCleared:bookings.length}}__name(revokeSmsConsent,\"revokeSmsConsent\");async function restoreSmsConsent(phone){const normalized=normalizePhone(phone);if(!normalized)return{ledgerRestored:0,bookingsRestored:0};const pb=(0,import_client.getPocketBaseClient)();const consentedAt=new Date().toISOString();const ledger=await pb.collection(import_collections.pocketBaseCollections.smsConsents).getFullList({filter:pb.filter(\"phone = {:phone}\",{phone:normalized})});if(ledger.length===0)return{ledgerRestored:0,bookingsRestored:0};for(const record of ledger){await pb.collection(import_collections.pocketBaseCollections.smsConsents).update(record.id,{revokedAt:\"\",consentedAt})}const bookings=await bookingsForPhone(normalized);for(const booking of bookings){await pb.collection(import_collections.pocketBaseCollections.bookings).update(booking.id,{smsConsentedAt:consentedAt})}return{ledgerRestored:ledger.length,bookingsRestored:bookings.length}}__name(restoreSmsConsent,\"restoreSmsConsent\");async function hasActiveSmsConsent(phone){const normalized=normalizePhone(phone);if(!normalized)return false;const record=await(0,import_core.first)(import_collections.pocketBaseCollections.smsConsents,\"phone = {:phone}\",{phone:normalized});return Boolean(record&&!record.revokedAt)}__name(hasActiveSmsConsent,\"hasActiveSmsConsent\");0&&(module.exports={classifyInboundKeyword,hasActiveSmsConsent,normalizePhone,recordSmsConsent,restoreSmsConsent,revokeSmsConsent});\n})()\n","warnings":[],"map":{"version":3,"mappings":";mvBAAA,kXAAoC,mCACpC,uBAAsC,wCACtC,gBAA0C,iCAQ1C,MAAM,iBAAmB,IAAI,IAAK,CAAE,OAAQ,UAAW,cAAe,SAAU,MAAO,OAAQ,SAAU,QAAS,CAAE,EACpH,MAAM,gBAAkB,IAAI,IAAK,CAAE,QAAS,SAAU,MAAO,OAAQ,CAAE,EACvE,MAAM,cAAgB,IAAI,IAAK,CAAE,OAAQ,MAAO,CAAE,EAE3C,SAAS,uBAAwB,KAAiC,CACvE,MAAM,QAAU,KAAK,KAAK,EAAE,YAAY,EAAE,QAAS,UAAW,EAAG,EACjE,GAAK,iBAAiB,IAAK,OAAQ,EAAI,MAAO,UAC9C,GAAK,gBAAgB,IAAK,OAAQ,EAAI,MAAO,SAC7C,GAAK,cAAc,IAAK,OAAQ,EAAI,MAAO,OAC3C,MAAO,SACT,CANgB,wDAUT,SAAS,eAAgB,MAAwB,CACtD,MAAM,OAAS,MAAM,QAAS,MAAO,EAAG,EACxC,OAAO,OAAO,OAAS,GAAK,O
G,WAAY,yCAAsB,WAAY,EAAE,YAAa,CACnF,OAAQ,GAAG,OAAQ,mBAAoB,CAAE,MAAO,UAAW,CAAE,CAC/D,CAAE,EAEF,GAAK,OAAO,SAAW,EAAI,MAAO,CAAE,eAAgB,EAAG,iBAAkB,CAAE,EAC3E,UAAY,UAAU,OAAS,CAC7B,MAAM,GAAG,WAAY,yCAAsB,WAAY,EAAE,OAAQ,OAAO,GAAI,CAAE,UAAW,GAAI,WAAY,CAAE,CAC7G,CAEA,MAAM,SAAW,MAAM,iBAAkB,UAAW,EACpD,UAAY,WAAW,SAAW,CAChC,MAAM,GAAG,WAAY,yCAAsB,QAAS,EAAE,OAAQ,QAAQ,GAAI,CAAE,eAAgB,WAAY,CAAE,CAC5G,CAEA,MAAO,CAAE,eAAgB,OAAO,OAAQ,iBAAkB,SAAS,MAAO,CAC5E,CArBsB,8CAuBtB,eAAsB,oBAAqB,MAAkC,CAC3E,MAAM,WAAa,eAAgB,KAAM,EACzC,GAAK,CAAC,WAAa,MAAO,OAC1B,MAAM,OAAS,QAAM,mBAAO,yCAAsB,YAAa,mBAAoB,CAAE,MAAO,UAAW,CAAE,EACzG,OAAO,QAAS,QAAU,CAAC,OAAO,SAAU,CAC9C,CALsB","names":[],"ignoreList":[],"sources":["/Volumes/Dev/www/Goldridr/website/src/lib/notifications/sms-consent.ts"],"sourcesContent":["import { getPocketBaseClient } from \"@/lib/pocketbase/client\";\nimport { pocketBaseCollections } from \"@/lib/pocketbase/collections\";\nimport { createWithLegacyId, first } from \"@/lib/pocketbase/core\";\n\n// Carriers require an auditable record of when each number consented and when it\n// revoked. The sms_consents collection is that ledger; bookings.smsConsentedAt is the\n// send-time gate that mirrors it.\n\nexport type SmsConsentAction = \"opt_in\" | \"opt_out\" | \"help\" | \"unknown\";\n\nconst OPT_OUT_KEYWORDS = new Set( [ \"stop\", \"stopall\", \"unsubscribe\", \"cancel\", \"end\", \"quit\", \"revoke\", \"optout\" ] );\nconst OPT_IN_KEYWORDS = new Set( [ \"start\", \"unstop\", \"yes\", \"optin\" ] );\nconst HELP_KEYWORDS = new Set( [ \"help\", \"info\" ] );\n\nexport function classifyInboundKeyword( body: string ): SmsConsentAction {\n  const keyword = body.trim().toLowerCase().replace( /[^a-z]/g, \"\" );\n  if ( OPT_OUT_KEYWORDS.has( keyword ) ) return \"opt_out\";\n  if ( OPT_IN_KEYWORDS.has( keyword ) ) return \"opt_in\";\n  if ( HELP_KEYWORDS.has( keyword ) ) return \"help\";\n  return \"unknown\";\n}\n\n// Booking phone numbers are free text while Twilio always sends E.164, so both sides\n// are reduced to their significant digits before comparison.\nexport function normalizePhone( phone: string ): string {\n  const digits = phone.replace( /\\D/g, \"\" );\n  return digits.length > 10 ? digits.slice( -10 ) : digits;\n}\n\nasync function bookingsForPhone( phone: string ): Promise<Array<{ id: string; passengerPhone: string }>> {\n  const normalized = normalizePhone( phone );\n  if ( normalized.length < 10 ) return [];\n  const pb = getPocketBaseClient();\n  const records = await pb.collection( pocketBaseCollections.bookings ).getFullList( {\n    filter: pb.filter( \"passengerPhone != ''\" ),\n    fields: \"id,passengerPhone\",\n  } );\n  return records\n    .map( record => ( { id: String( record.id ), passengerPhone: String( record.passengerPhone || \"\" ) } ) )\n    .filter( record => normalizePhone( record.passengerPhone ) === normalized );\n}\n\nexport async function recordSmsConsent( input: {\n  customerEmail: string;\n  phone: string;\n  consentVersion: string;\n  consentedAt?: string;\n} ): Promise<void> {\n  const normalized = normalizePhone( input.phone );\n  if ( !normalized || !input.customerEmail ) return;\n  const consentedAt = input.consentedAt || new Date().toISOString();\n  const existing = await first(\n    pocketBaseCollections.smsConsents,\n    \"phone = {:phone} && customerEmail = {:email}\",\n    { phone: normalized, email: input.customerEmail }\n  );\n  if ( existing ) {\n    await getPocketBaseClient().collection( pocketBaseCollections.smsConsents ).update( existing.id, {\n      consentVersion: input.consentVersion,\n      consentedAt,\n      revokedAt: \"\",\n    } );\n    return;\n  }\n  await createWithLegacyId( pocketBaseCollections.smsConsents, {\n    customerEmail: input.customerEmail,\n    phone: normalized,\n    consentVersion: input.consentVersion,\n    consentedAt,\n    revokedAt: \"\",\n    sourceCreatedAt: consentedAt,\n  } );\n}\n\nexport async function revokeSmsConsent( phone: string ): Promise<{ ledgerRevoked: number; bookingsCleared: number }> {\n  const normalized = normalizePhone( phone );\n  if ( !normalized ) return { ledgerRevoked: 0, bookingsCleared: 0 };\n  const pb = getPocketBaseClient();\n  const revokedAt = new Date().toISOString();\n\n  const ledger = await pb.collection( pocketBaseCollections.smsConsents ).getFullList( {\n    filter: pb.filter( \"phone = {:phone}\", { phone: normalized } ),\n  } );\n  for ( const record of ledger ) {\n    await pb.collection( pocketBaseCollections.smsConsents ).update( record.id, { revokedAt } );\n  }\n\n  const bookings = await bookingsForPhone( normalized );\n  for ( const booking of bookings ) {\n    await pb.collection( pocketBaseCollections.bookings ).update( booking.id, { smsConsentedAt: \"\" } );\n  }\n\n  return { ledgerRevoked: ledger.length, bookingsCleared: bookings.length };\n}\n\nexport async function restoreSmsConsent( phone: string ): Promise<{ ledgerRestored: number; bookingsRestored: number }> {\n  const normalized = normalizePhone( phone );\n  if ( !normalized ) return { ledgerRestored: 0, bookingsRestored: 0 };\n  const pb = getPocketBaseClient();\n  const consentedAt = new Date().toISOString();\n\n  const ledger = await pb.collection( pocketBaseCollections.smsConsents ).getFullList( {\n    filter: pb.filter( \"phone = {:phone}\", { phone: normalized } ),\n  } );\n  // A number that never consented cannot opt itself back in by texting START.\n  if ( ledger.length === 0 ) return { ledgerRestored: 0, bookingsRestored: 0 };\n  for ( const record of ledger ) {\n    await pb.collection( pocketBaseCollections.smsConsents ).update( record.id, { revokedAt: \"\", consentedAt } );\n  }\n\n  const bookings = await bookingsForPhone( normalized );\n  for ( const booking of bookings ) {\n    await pb.collection( pocketBaseCollections.bookings ).update( booking.id, { smsConsentedAt: consentedAt } );\n  }\n\n  return { ledgerRestored: ledger.length, bookingsRestored: bookings.length };\n}\n\nexport async function hasActiveSmsConsent( phone: string ): Promise<boolean> {\n  const normalized = normalizePhone( phone );\n  if ( !normalized ) return false;\n  const record = await first( pocketBaseCollections.smsConsents, \"phone = {:phone}\", { phone: normalized } );\n  return Boolean( record && !record.revokedAt );\n}\n"]}}